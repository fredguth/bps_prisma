/** @type {import('./$types').RequestHandler} */
export async function GET() {
  return new Response();
}

// export const reload = async ({ params, pageNumber, rowsPerPage }) => {
//   const { id } = params;

// }
// 607262;
